import { greet, num, student, sum } from "./Helpers/helpers";

function App() {

console.log(student);
console.log(greet(student));

console.log(sum(num, 5));

  return (
    <div className="App">
      
    </div>
  );
}

export default App;
